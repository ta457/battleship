function Ship(length) {
  this.length = length;
  this.hits = [];

  const hit = (hitPosition) => {
    
  }

  const isSunk = () => {

  }

  const getLength = () => this.length;

  return {  }
}

module.exports = Ship;